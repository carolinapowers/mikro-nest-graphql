const config = {
  entities: ['dist/**/*.entity.js'],
  entitiesTs: ['src/**/*.entity.ts'],
  dbName: 'testdb',
  type: 'postgresql',
  host: 'localhost',
  port: 5432,
  user: 'meg-kline-tuls',
  password: '',
  debug: true,
};

export default config;
